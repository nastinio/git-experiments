public class Hello {
  public static void main(String[] args) {
    System.out.println("Йоу!");

    /*System.out.println(resList.get(0));

    Date[] dates = {null, date1, date2, date3, null};
    for (Date date : dates) {
      if (date != null){
        if (datemin == null || date.before(datemin)) {
          datemin = date;
        }
      }
    }

    System.out.println(datemin);*/
    /*// Прочекать, сколько тредов создастся
    Collection<Integer> docs = new ArrayList<>();
    for (int i = 0; i < 1000; i++) {
      docs.add(i);
    }

    // Посчитать сумму всех элементов прокручивающихся в потоке
    ExecutorService service = Executors.newFixedThreadPool(5);
    // По сути execute на вход получает поток, который обрабатывает
    docs.forEach(doc -> service.execute(new SomeTask(doc)));*/

  }


}